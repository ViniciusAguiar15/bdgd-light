# PRs #74, #75, #76, #77 e #78 — correções pedidas nas revisões PR-20 a PR-24

**Veredito: todos aprovados. Os 10 pedidos das revisões da rodada 4 foram atendidos.**

Commits em `main`: `b039acc` (#74), `c6a3f92` (#75), `19b6c81` (#76), `64964da` (#77), `b77fc92` (#78).

## O que foi corrigido

| PR | Pedido | Como ficou |
|---|---|---|
| #74 | PR-20 §1 — rejeição vaga não restringia nada | `_garantir_chave_rejeitada` aplicada nas **quatro** saídas de `_extrair_restricao_rejeicao` (não só no fallback): toda rejeição impede ao menos a reproposição da mesma chave. Era o pedido, e foi implementado na forma mais forte. |
| #74 | PR-20 §2 — `_match_alimentador` por substring | casamento endurecido; o casamento aproximado passa a ser sinalizado. |
| #74 | PR-20 §3 — restrição invisível no console | `resumo_restricoes()` aparece junto do aviso de replanejamento. |
| #75 | PR-21 §1 — **DEC inflado** | `_impacto_dec_conjunto` passou a receber `minutos_evitar_por_cliente` e calcula `restauradas × minutos` **daquele conjunto**; `consumidor_minutos_evitados` segue como total do evento. É a correção exata: o numerador agora casa com o denominador. |
| #75 | PR-21 §2 — premissa de manobra ausente na tela | premissa completa (reparo + manobra) no cartão. |
| #76 | PR-22 §1 — número elétrico sem data | a simulação é carimbada e o console mostra quando foi feita. |
| #76 | PR-22 §2 — clique inerte em trecho sem `cod_id` | tratado. |
| #77 | PR-23 §1 — conclusão do A/B em falso dilema | reescrita como **interação**: "o gatilho é o texto dos exemplos, mas só no Gemini Flash", com os números dos dois braços na mesma frase. Agora bate com a tabela. |
| #77 | PR-23 §2 — limitação frente ao PowerChain | parágrafo explícito: a rodada mede se os exemplos *custam* acerto, não se *rendem*; não confirma nem refuta a premissa do PowerChain. |
| #77 | PR-23 §3 — qual provedor a demo usa | `docs/bench.md` e `docs/demo-roteiro.md` dizem que, com as duas chaves no ambiente, a resolução para em `OPENAI_API_KEY`. |
| #78 | PR-24 §1 e §2 — tipo de `PN_CON` e os 2 km sem justificativa | normalização aplicada e constante justificada, com teste novo em `test_recorte.py`. |

## Observação de processo

As revisões PR-20 a PR-24 foram escritas na árvore de trabalho sem commit e sumiram dela antes de
serem versionadas (limpeza de arquivos não rastreados na sessão do CLI), junto com o diário
`NOITE-4.md`. **Nada se perdeu**: o Copilot recuperou os cinco textos e o diário e os versionou no
PR #73 (`8765d5b`), e os PRs #74–#78 os citam pelo nome nas mensagens de commit.

Regra para as próximas rodadas, ainda assim: **revisão escrita é revisão commitada**, no mesmo passo
em que é escrita — a recuperação dependeu de o Copilot ainda ter os textos em contexto.
