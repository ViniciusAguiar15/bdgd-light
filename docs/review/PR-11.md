# Revisão — PR #42 `feat/twin-score-eletrico` (issue #18)

**Veredito: aprovado.** `score_eletrico` faz o que o verificador precisa: um fluxo por opção sobre o Master do GPKG,
perímetro = fonte receptora + nós transferidos, ordenação viável → margem do disjuntor → UCBT, `motivos` sempre
explicando o "não". A honestidade sobre `COR_NOM` (código sem tabela → não chutar, usar ampacidade do tronco com
`nominais=` para sobrepor) é a decisão correta. 187 testes.

O achado é ouro para a demo: **AMALIA (URG29983) é inviável por um trecho de 20 m a 180 %** (`11051956`, condutor
CNOM 132 A) que o grafo não enxerga e o gêmeo enxerga — exatamente o argumento "verificador determinístico por cima
do LLM" do PowerChain/PDF do COD. Vai para o roteiro da apresentação.

## Pedidos
1. Tabela de domínio `COR_NOM`: o Manual da BDGD (Módulo 10, anexo de domínios "CORNOM"/"TCORNOM") a traz; se o
   mantenedor tiver o PDF, transcrever para `catalogo.py` e usar como `i_nominal_a` primário (pendência 1 já anotada).
2. Registrar em `docs/escopo-cidade.md` (cenário A) o ranking elétrico: RIMARAES viável 46 % de margem, BOMPASTOR viável
   20 %, AMALIA inviável (gargalo `11051956`) — e a pergunta aberta "cadastro ou restrição real?".
3. Pendência 2 (barra que força `vminpu=0.9` em Tijuca): abrir issue #44 "diagnóstico de convergência" para depois.
