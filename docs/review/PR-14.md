# Revisão — PR #46 `feat/agente-hitl` (issue #34)

**Veredito: aprovado — é o marco da fase 3.** Os quatro pontos que eu marquei como críticos foram atendidos e
testados: `set_switch` e `load_cluster` nunca chegam ao modelo; o verificador (12 checagens determinísticas + veredito
do gêmeo) é um *gate* dentro de `propose_plan` que devolve `problemas` para replanejar (padrão PowerChain); a auditoria
registra perfil/modelo (`llm.cliente`, pedido PR-09), tokens e tempos separando LLM de ferramentas; e AMALIA foi
descartada pelo gargalo de 180 %. Validação real com Gemini nos três cenários: proposta correta em 5 rodadas, zero
recusas, Ipanema tratado como "isolar e despachar". Exemplos anotados em YAML com seleção lexical e bônus categórico
(boa observação sobre o Jaccard puro). `fake_operador()` como baseline determinístico. 239 testes.

## Pedidos (para o #36 benchmark, já apontados pelo próprio diário)
1. **Custo**: 50–65 k tokens por falta é alto. Compactar o retorno de `restore_options` (top-N com score, resumo dos
   demais) e de `get_topology` (contagens, não listas) — medir antes/depois no benchmark.
2. Rodar os três cenários com `--provider openai` assim que a chave estiver no ambiente (linha da tabela em
   `docs/agent.md`); o benchmark deve ter OpenAI × Gemini × fake.
3. Registrar em `docs/agent.md` um exemplo de **recusa do verificador** forçada (ex.: exemplo anotado propositalmente
   errado ou `--vmin 0.99`) para mostrar o ciclo de replanejamento funcionando — hoje as três execuções reais tiveram
   zero recusas e a demo precisa desse momento.
