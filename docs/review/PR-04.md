# Revisão — issue #5 `feat/twin-opendss` (gêmeo OpenDSS) — inclui ajustes do PR-03

**Veredito: aprovado.** Revisão às 02:10 UTC, modo noturno. É o marco técnico da noite: o cenário FLISR do escopo v2
roda de ponta a ponta (grafo → manobras → DSS → fluxo) em ~1,2 s, com as duas rotas de restauração viáveis
(via PARNAIBA 221 A / via CURUMAU 262 A, MT ≥ 1,004 pu, 0 sobrecargas MT).

## O que está bom
- bdgd2opendss funcionou sem ajuste (TQR0007 em 180 s; cluster em 310 s), conversor próprio dispensado.
- Não convergência bruta tratada com cascata **registrada** (`maxiterations` → `vminpu=0.9` → `model=2`), e a causa
  raiz (RAMLIG.COMP de 700–900 m em ramais de 220 V) documentada sem "corrigir" o dado — postura certa.
- `twin/cluster.py`: Master do cluster com `Vsource` por CTMT + `comandos_manobras` traduzindo a sequência do grafo
  para DSS (jumper até `PAC_VIZ`). Fixture `cluster_mini` fecha o teste ponta a ponta sem dados reais. 23 testes.
- Pedidos do PR-03 (to_dict, `manobras`, nota de capacidade) aplicados em commit próprio.

## Pedidos (para a fase 3, não bloqueiam nada agora)
1. `twin.score_eletrico(opcoes, rede, master_cluster)`: para cada `OpcaoRestauracao`, rodar o fluxo com a manobra e
   devolver `{convergiu, I_disjuntor_A, margem_disjuntor, Vmin_MT_pu, sobrecargas_MT, perdas_kW}` + veredito
   `viavel` (limites 0,93–1,05 pu e I ≤ nominal do disjuntor/condutor). É o "verificador" do agente.
2. Master a partir do GPKG do recorte (decisão 9 do spike) continua desejável para grafo e gêmeo serem a mesma
   rede; pode ficar como issue nova (#17) na fila pós-noite.
3. Anotar no README/escopo que a BT do modelo tem ramais suspeitos (dado BDGD) e que as métricas de decisão
   do MVP são **MT** (tensão MT, corrente no disjuntor/tronco), não BT.
