# Revisão — PR #37 `feat/escopo-v3-clusters` (issue #30)

**Veredito: aprovado.** Três achados de dados/ferramenta que valem mais que a issue em si: (1) bug de tie
"ambígua" no grafo (anel interno casando com CTMT externo) corrigido com `via_chave`; (2) **cenário B de Ipanema
não existe** — 35/36 NA do LDS 9210 são o pátio da SE (barra não modelada) → `EM_SUB` com folga de 50 m e Ipanema
vira cenário negativo, decisão correta e didática; (3) **bug do bdgd2opendss com bancos monofásicos `DF`**
(3 unidades em delta com barras de 2 nós → perdas de 64 %) corrigido por `corrigir_bancos_monofasicos`.
Tijuca fecha o cenário A: base 13,3 MW, falta no tronco de CABOFRIO, três restaurações viáveis (RIMARAES < BOMPASTOR
< AMALIA por tensão/perdas). 169 → 174 testes.

## Pedidos
1. Abrir a issue no `PauloRadatz/bdgd2opendss` com o caso mínimo `tests/fixtures/dss/bancos_monofasicos.dss`
   (Copilot pode fazer via `gh issue create -R PauloRadatz/bdgd2opendss`); referenciar no `spike-opendss.md`.
2. Pendências 1 e 2 do fechamento (regerar inventário com folga `EM_SUB`; filtrar PONNOT/UCBT pelo bbox do
   cluster) viram issues próprias (#39, #40) para a fila pós-fase 3; regerar o inventário pode rodar em
   background durante outra issue (≈ minutos, não horas, se usar `--layers` só das camadas necessárias).
3. `docs/escopo-cidade.md`: marcar explicitamente que a tabela por região está **desatualizada** para
   ties de SE até o inventário ser regerado (uma linha no topo já basta).
