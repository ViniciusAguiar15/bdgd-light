# Revisão — issue #4 `feat/console` (PR #20) e ajustes PR-04/05/06 (PR #19)

**Veredito: aprovado.** Console MapLibre + PMTiles com simbologia de operador (MT por tensão, NA/NF, telecomandadas,
ties com CTMT vizinho), sobreposição do estado do grafo via `?estado=` e smoke headless por CDP que achou dois bugs
reais (worker do maplibre no Vite; `match` com 13.2). `bdgd-light tiles` gera o cluster em 1,4 s / 1,43 MB.
ADR-002 escrita sem editar a ADR-001. `score_eletrico` (#18) e Master do recorte (#17) abertas como issues.

## Único critério parcial
GitHub Pages não pode ser ativado em repositório privado no plano atual (HTTP 422). Decisão do mantenedor:
tornar o repo público (dados nunca entraram no git, então é seguro), ou manter privado e usar
`npm run dev` local para a demo. O workflow já está pronto para os dois casos.

## Pedidos (fase 2/3, sem urgência)
1. Legenda flutuante e ícones das chaves BT (já anotado pelo próprio diário).
2. `?estado=` deve aceitar também o JSON de `bdgd-light dss --json` para colorir sobrecargas e tensão MT (o
   verificador da F3 vai produzir isso).
3. Painel de eventos/fila de aprovação fica para o console da F3 (depois do servidor MCP), não agora.
