# Revisão — issue #7 `feat/llm-client` (LLMClient)

**Veredito: aprovado.** Achado importante e bem documentado: **GitHub Models foi aposentado em 30/07/2026**
(HTTP 410); a decisão de provedor passa para o mantenedor (ver `docs/spike-llm.md`). Interface `LLMClient`
+ `OpenAICompatClient` (Azure AI Foundry, OpenAI, Ollama) + `FakeLLMClient` cobrem qualquer escolha; 28 testes
sem rede; segredos só por ambiente com teste que varre o código.

## Para o mantenedor decidir (manhã)
- Provedor real do agente. Opções compatíveis com o cliente já escrito: Azure AI Foundry (modelos OpenAI e
  Claude via Foundry), OpenAI direto, ou a API da Anthropic pelo endpoint de compatibilidade OpenAI
  (`https://api.anthropic.com/v1/chat/completions` com `BDGD_LLM_TOKEN` = chave Anthropic — conferir na
  documentação se o tool calling é suportado nesse modo; senão, um `AnthropicClient` nativo de ~60 linhas
  via httpx). Ollama local serve para desenvolvimento offline.

## Pedido pequeno
- `conversar()` deve gravar cada rodada (mensagens, tool calls, resultados, uso de tokens) no formato do log de
  auditoria definido na ADR-001 (JSON Lines com hash do anterior) — pode ser um `AuditLog` mínimo em
  `bdgd_light.agent.audit`, já usado pelo `bdgd-light llm`. Base do RACI-A e do benchmark (tokens/pass@1).
