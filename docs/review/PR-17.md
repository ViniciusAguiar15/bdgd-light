# Revisão — PR #54 `feat/demo-passo-a-passo` (issue #51)

**Veredito: aprovado.** Modo passo a passo sem tocar no servidor MCP (o `set_switch` já impunha a ordem; só entrou
`humano.executar_passo` + rota `/passo`, token nunca vai ao navegador), mapa recolorindo a cada manobra (467 → 467 → 72),
smoke em etapas com capturas (`docs/img/1-evento … 3-executada`), `docs/demo-roteiro.md` testado do zero num clone
(uv sync 1 s com cache, serve em 2 s, demo passo a passo em 17 s com fake; 27 s com Gemini). 276 testes.

## Observações
- `OPENAI_API_KEY` **de novo ausente** no ambiente do Copilot (ele procurou em zshrc/zprofile/.env/launchctl/keychain).
  O mantenedor exportou no próprio terminal, mas não no shell que abriu o Copilot. Nada a fazer pelo Copilot; a #52
  fica com OpenAI pendente e o roteiro cita os números validados pelo mantenedor em 10/09.
- GIF ficou como 3 PNG por falta de `gifski`/`ffmpeg`: aceitável (critério alternativo). Se o mantenedor quiser o GIF:
  `brew install gifski` e o comando está na pendência da §1.
- Porta 8000 ocupada por Docker na máquina → roteiro usa 8010; deixar isso explícito no roteiro (se já não está).
