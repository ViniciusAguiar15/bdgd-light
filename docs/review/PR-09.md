# Revisão — PR #38 `feat/adr-003-provedor-llm` (issue #31)

**Veredito: aprovado.** Perfis `openai|gemini|ollama|fake` com precedência clara, `Conversa.segundos` para o
benchmark, job `llm-smoke` condicionado a segredo, varredura de segredos cobrindo `AIza`. Validação real com
Gemini: 277 tokens, 1,8 s, ≈ US$ 0,00014 por conversa; `gemini-2.5-flash-lite` devolve mensagem vazia com tools —
bem registrado como motivo para não ser padrão.

## Para o mantenedor
- `OPENAI_API_KEY` não estava no shell do Copilot; rodar `uv run bdgd-light llm --provider openai "Quanto é 2 + 3?"`
  e preencher a linha `openai` em `docs/spike-llm.md` §6; `gh secret set OPENAI_API_KEY` e `GEMINI_API_KEY`.

## Pedido pequeno (próxima issue que tocar `agent/`)
- `cliente_do_ambiente` deve registrar no `AuditLog` qual perfil/modelo foi resolvido (hoje só aparece no
  terminal), para o benchmark saber a proveniência de cada execução.
