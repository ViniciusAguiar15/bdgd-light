# Revisão — PR #49 `feat/benchmark` (issue #36)

**Veredito: aprovado.** Harness com gabarito recalculado em tempo de execução (nunca lido do YAML), conjunto de chaves
equivalentes como gabarito de proposta, tolerâncias corretas (contagens exatas), métricas do PowerChain (pass@1,
pass@k não enviesado, ordem, precisão, tokens/pass@1) mais `chars_ferramentas` como proxy de custo do fake.
Compactação medida: −74 % de chars por execução. Achado importante: o `MALFORMED_FUNCTION_CALL` do Gemini era causado
pela compactação (manobras viradas texto) — diagnosticado por A/B e corrigido mantendo objetos; hard passou de 13/20 a
19/20. Pedidos PR-14/PR-15/RESULTADOS-OPENAI incorporados (recusa forçada documentada com `--vmin 1.01`, critério de
correção numérica do resumo, motivo de descarte). 275 testes.

## Pedidos (rodada 3)
1. OpenAI no benchmark (chave agora disponível no shell do mantenedor — exportar antes de abrir o Copilot).
2. Pendências 3–5 do diário (Gemini k=5 nas hard, tarefas de Ipanema, eventos transitório/chave indisponível, prompt
   exigindo o número explícito na resposta final).
