# Revisão — PR #41 `feat/twin-master-gpkg` (issue #17)

**Veredito: aprovado.** Escolha certa: conversor próprio (`twin/gpkg2dss.py`) validado por **paridade exata** com o
bdgd2opendss em TQR0007 (mesmas 6.334 barras, 12.312 cargas, 264,335 kW de perdas, |ΔV| = 0 em 14.695 nós), usando o
pacote como oráculo em vez de dependência. Grafo e gêmeo agora leem a mesma rede (o GPKG). Bônus: descobriu que faltava
`PIP` (iluminação pública, 1.235 cargas em TQR0007) no recorte e um segundo bug upstream (perdas 3× em bancos DF/DA),
ambos reportados (bdgd2opendss #35 e #36). 182 testes.

## Pedidos (aplicar no início da #18)
1. `docs/spike-opendss.md` ou `docs/twin.md`: uma seção "Quando usar `--gdb` × `--gpkg`" — o padrão do projeto passa
   a ser `--gpkg` (rede = recorte); `--gdb` só para regressão de paridade. Atualizar README e exemplos nesse sentido.
2. Paridade como teste de regressão contínuo: manter `test_gpkg2dss.py::paridade` em `skipif` sem dados, e adicionar
   ao `NOITE-2.md`/README o comando para o mantenedor rodá-lo após regerar recortes.
3. Lembrete: a #18 (`score_eletrico`) deve rodar sobre o Master do GPKG (`data/dss/gpkg/`), não sobre `data/dss/`.
